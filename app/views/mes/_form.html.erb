<%= bootstrap_form_for(@me, as: :user, url: me_path, html: {method: :put}) do |f| %>
  <% if @me.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@me.errors.count, "error") %> prohibited this me from being saved:</h2>

      <ul>
      <% @me.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :first_name %>
  <%= f.text_field :last_name %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
